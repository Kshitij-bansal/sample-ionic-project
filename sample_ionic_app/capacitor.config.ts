import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sample_ionic_app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
